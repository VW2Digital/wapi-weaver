# META V3 — ENV FINAL VALIDATION REPORT

## MIGRATION SEQUENCE

043 conflict: NO

- `database/migrations/040_*.sql` exists
- `database/migrations/041_*.sql` exists
- `database/migrations/042_*.sql` exists
- `database/migrations/043_add_meta_config_id_to_meta_app_connections.sql` created, no conflict

## META_CONFIG_ID SCHEMA

- `meta_app_connections.meta_config_id` added via migration 043
- `database/schema/required-columns.json` updated
- `canonical-schema.sql` not updated (table `meta_app_connections` not present in canonical file; documented as known gap)

## OLD GLOBAL META ENV

- `VITE_META_APP_ID`: REMOVED (only in comments/docs/skills)
- `VITE_META_CONFIG_ID`: REMOVED (only in comments/docs/skills)
- `META_APP_SECRET`: REMOVED (only in comments/docs/skills)
- `META_WEBHOOK_VERIFY_TOKEN`: REMOVED (only in comments/docs/skills)

## ENV EXAMPLE

PASS

```text
No VITE_META_APP_ID
No VITE_META_CONFIG_ID
No META_APP_SECRET
No META_WEBHOOK_VERIFY_TOKEN
META_CREDENTIALS_ENCRYPTION_KEY present
```

## INSTALL.SH

PASS

- No generation of Meta App ID, Config ID, App Secret, Webhook Verify Token
- Generates `META_CREDENTIALS_ENCRYPTION_KEY` with `openssl rand -hex 32` if absent
- Does not print the key to terminal
- Warns that Meta credentials are per-tenant

## DOCKER COMPOSE

- `docker-compose.yml` and `docker-compose.production.yml` do not pass `VITE_META_*` or `META_APP_SECRET` or `META_WEBHOOK_VERIFY_TOKEN` explicitly
- They load `.env` via `env_file`; `.env` generated by `install.sh` is clean
- `META_CREDENTIALS_ENCRYPTION_KEY` is not baked; injected at runtime from `.env`

## DOCKERFILE

- No build args for `VITE_META_APP_ID`, `VITE_META_CONFIG_ID`, `META_APP_SECRET`, `META_WEBHOOK_VERIFY_TOKEN`
- `APP_GIT_SHA` and `APP_GIT_BRANCH` are the only build args
- `META_CREDENTIALS_ENCRYPTION_KEY` is not baked in image; runtime only

## META_CREDENTIALS_ENCRYPTION_KEY

- Backend (app container): available via `.env` `env_file`
- Worker: same container/`.env` at runtime
- Frontend: NOT exposed (no `VITE_` prefix, not in `env` build args)

## FAIL CLOSED

- `encryptMetaCredential` / `decryptMetaCredential` throw `FAIL_CLOSED: META_CREDENTIALS_ENCRYPTION_KEY is not configured` when missing
- `saveMetaAppConnection` uses `encryptMetaCredential` -> fail-closed

## DECRYPT WITH VALID KEY

- Unit tests `tests/jest/messaging/integration.jest.test.ts` PASS
- No runtime test executed (no running local DB in this session)

## SETTINGS FRONTEND

- `src/routes/_app/settings.tsx` now fetches `appId` and `configId` from `getDefaultMetaAppConnectionForEmbeddedSignup` server function
- No `import.meta.env.VITE_META_*` used

## WHATSAPP BUSINESS PROFILE

- `onboardWhatsApp` resolves `meta_app_connections` for the tenant
- App ID / App Secret are taken from the connection, not from ENV
- Multi-connection scenario: currently falls back to newest connection; explicit `meta_app_connection_id` accepted in payload

## CHANNEL.SERVICE

- `getMessengerChannelConfig` no longer returns `process.env.META_APP_SECRET`
- `appSecret` returned as `null` (legacy path still uses `facebook_pages` token; needs follow-up for V3 wiring)

## WEBHOOK V3

- `/api/public/meta-webhook/$publicId` uses `meta_app_connections.app_secret_encrypted`
- No `process.env.META_APP_SECRET` fallback
- Verify token is `meta_app_connections.webhook_verify_token_encrypted`
- No `process.env.META_WEBHOOK_VERIFY_TOKEN` fallback

## LEGACY WEBHOOK REGRESSION

- Legacy webhooks (`whatsapp-webhook`, `instagram-webhook`, `facebook-webhook`) still use `platform-config.service.ts`
- `platform-config.service.ts` no longer reads `process.env` fallbacks
- Legacy tenants with `profiles.whatsapp_app_secret` / `instagram_accounts.app_secret` still supported from DB

## SCRIPTS

- `scripts/setup-webhook-test.mjs`: UPDATED — creates V3 `meta_app_connections` fixture with encrypted secrets
- `scripts/test-webhooks/send-webhook.mjs`: UPDATED — posts to `/api/public/meta-webhook/$publicId` with explicit test `appSecret` argument

## GLOBAL RUNTIME SEARCH (src/)

```text
process.env.META_APP_SECRET: 0 (only comments)
process.env.META_WEBHOOK_VERIFY_TOKEN: 0 (only comments)
import.meta.env.VITE_META_APP_ID: 0
import.meta.env.VITE_META_CONFIG_ID: 0
```

## BUILD

PASS

```text
npm run build -> ✓ built in 29.58s
```

## TYPECHECK

PASS

```text
npm run type-check -> exit code 0
```

## TESTS

```text
npm test -- tests/jest/messaging -> PASS (6 passed, 2 skipped)
```

## GIT STATUS

- Modified: `.env.example`, `install.sh`, `platform-config.service.ts`, `channel.service.ts`, `whatsapp-business-profile.functions.ts`, `settings.tsx`, `profile.functions.ts`, `required-columns.json`, `setup-webhook-test.mjs`, `send-webhook.mjs`
- New: `database/migrations/043_add_meta_config_id_to_meta_app_connections.sql`
- No `.env` real was committed or modified with actual secrets (only `.env.example`)

## LEGACY DATA DELETED

NO

## DESTRUCTIVE OPERATIONS

NONE

## FINAL STATUS

ENV ARCHITECTURE CORRECT FOR MULTI-TENANT SAAS — V3 META APP CONNECTIONS ARE THE AUTHORITATIVE SOURCE

Remaining known items (no destructive action):

- Update `docs/CONFIGURACAO-META.md` and `docs/SEGURANCA.md` to remove references to global `META_APP_SECRET`.
- Audit `canonical-schema.sql` for `meta_app_connections` table parity (out of scope for this cutover).
- Runtime V3 GET/POST test requires running local stack with fixture.
